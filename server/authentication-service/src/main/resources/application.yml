server:
  port: 8083

spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: "jdbc:mysql://localhost:3306/chess"
    username: "root"
    password: "password"

security:
  jwt:
    token:
      secret-key: secret-key
      expire-length: 300000